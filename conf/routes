# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Serve index page from public directory

GET         /:url                            controllers.UrlController.shorten(request: Request, url: String)
GET         /url/error                       controllers.UrlController.error(request: Request)

